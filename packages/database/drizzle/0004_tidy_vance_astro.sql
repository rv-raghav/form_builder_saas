-- This migration was accidentally generated as a full schema dump duplicating
-- migrations 0000 through 0003. Keep it as a no-op so existing databases can
-- advance their Drizzle journal without recreating already-existing objects.
SELECT 1;
